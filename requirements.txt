# ─── Framework principal ──────────────────────────────────────────────────────
Django==6.0.3

# ─── Autenticación con Google ──────────────────────────────────────────────────────
django-allauth==65.3.0
requests==2.32.3
PyJWT==2.10.1
cryptography==44.0.3

# ─── Procesamiento de imágenes ────────────────────────────────────────────────
Pillow==12.1.1

# ─── Exportación de reportes ──────────────────────────────────────────────────
openpyxl==3.1.5
reportlab==4.4.10

# ─── Seguridad ────────────────────────────────────────────────────────────────
django-environ==0.13.0       # Variables de entorno (.env) — SECRET_KEY, DEBUG, DB
django-axes==8.3.1           # Bloqueo de fuerza bruta en login
django-csp==4.0              # Content Security Policy (cabeceras HTTP)

# ─── Rendimiento y archivos estáticos ────────────────────────────────────────
whitenoise==6.12.0           # Servir archivos estáticos en producción sin Nginx

# ─── Auditoría ────────────────────────────────────────────────────────────────
django-simple-history==3.11.0  # Historial de cambios en modelos (Docente, Estudiante, RecargaSaldo)

# ─── Observabilidad ───────────────────────────────────────────────────────────
structlog==25.5.0            # Logging estructurado (JSON en producción)

# ─── Formularios ──────────────────────────────────────────────────────────────
django-widget-tweaks==1.5.0  # Añadir clases CSS a widgets Django en templates

# ─── Desarrollo ───────────────────────────────────────────────────────────────
django-debug-toolbar==6.3.0  # Panel SQL/queries en desarrollo (solo DEBUG=True)

# ─── Inteligencia Artificial ──────────────────────────────────────────────────
anthropic>=0.40.0            # SDK oficial de Claude (Anthropic)

# ─── Pagos ────────────────────────────────────────────────────────────────────
mercadopago>=2.2.0           # SDK oficial de Mercado Pago

# ─── Despliegue ───────────────────────────────────────────────────────────────
gunicorn==25.3.0             # Servidor WSGI de producción
psycopg2-binary>=2.9.0       # Adaptador PostgreSQL para Railway
psycopg[binary]>=3.1.0       # Adaptador PostgreSQL moderno (requerido por Railway)
dj-database-url>=2.1.0       # Parsea DATABASE_URL a config Django
python-dotenv>=1.0.0         # Carga variables desde .env
cloudinary>=1.35.0           # SDK Cloudinary para subida de imágenes
django-cloudinary-storage>=0.3.0  # Backend Django Storage para Cloudinary

# ─── Carnets QR ───────────────────────────────────────────────────────────────
qrcode[pil]==8.2             # Genera códigos QR de los carnets de estudiantes

# ─── Google Calendar API ──────────────────────────────────────────────────────
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
